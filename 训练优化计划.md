# PPO 训练代码完善计划

## 一、当前问题分析

### 1.1 环境配置写死问题（鲁棒性风险）

当前 `train_ppo.py` 中环境配置固定：

```python
MAP_CONFIG = dict(
    type="block_num",  # 地图类型写死
    config=3,           # 地图规模写死
    lane_width=4,        # 车道宽度写死
    lane_num=1,          # 车道数量写死
)
```

**问题**：
- 训练时只使用单一地图布局
- 测试/比赛时如果地图不同，模型泛化能力差
- 无法适应不同路况条件

**解决方向**：
- 增加地图随机化配置
- 训练时使用多种地图变体
- 评估时测试不同地图配置

---

## 二、完善计划

### 阶段一：环境随机化（提升鲁棒性）

#### 1. 地图配置随机化
- 支持多种 `generate_type`（block_num, big_block_sequence 等）
- `config` 参数随机范围（如 2-5）
- `lane_width` 可选范围（如 3.5-4.5）
- `lane_num` 可选（如 1-2）

#### 2. 场景种子随机化
- 使用 `seed` 参数确保可复现性
- 训练时使用不同的 `env_seed`

#### 3. 难度渐进式提升
- 初期：简单地图（少车道、宽路）
- 后期：复杂地图（多车道、窄路、交叉口）

### 阶段二：训练策略优化

#### 1. 课程学习（Curriculum Learning）
- 从简单场景开始，逐渐增加难度
- 每个难度级别训练一定步数后升级

#### 2. 多环境并行
- 使用 `make_vec_env` 并行多个环境
- 不同环境使用不同地图配置

#### 3. 奖励塑形优化
- 当前奖励函数可能需要调优
- 增加对偏离路线的惩罚
- 增加对到达目标的奖励

### 阶段三：评估与验证

#### 1. 多地图测试
- 在多种地图配置下评估模型
- 统计到达率、平均步数等指标

#### 2. 压力测试
- 随机地图生成
- 极限条件测试（如最小车道宽度）

---

## 三、推荐实施顺序

1. **立即修复**：环境随机化配置（阶段一）
2. **短期优化**：课程学习 + 奖励调优（阶段二）
3. **长期完善**：完整评估体系（阶段三）

---

## 四、关键文件

- `train_ppo.py` - 主训练脚本
- `config.yaml` - 配置文件
- `reward_function.py` - 奖励函数
- `test_ppo.py` - 测试脚本
