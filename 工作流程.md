# ROBOTAC 复赛工作流程

## 环境准备

### robotac 环境（推荐）

```bash
# 激活环境
conda activate robotac

# 安装依赖（如需重新安装）
pip install metadrive_simulator==0.4.3
pip install torch stable-baselines3
```

### 官方嵌入式环境

```bash
cd python-3.7.0-embed-amd64
python.exe 你的脚本.py
```

---

## 复赛流程

### 1. 环境测试

```bash
conda activate robotac
python workspace/3-手动控制程序编写.py
```

控制按键：`I`前进 `K`刹车 `J`左转 `L`右转

### 2. 数据采集（可选）

编写脚本采集摄像头图像数据用于训练分析。

### 3. 模型训练

```bash
conda activate robotac
python train.py  # 你的训练脚本
```

需要安装：
```bash
pip install torch stable-baselines3
```

### 4. 比赛提交

1. 训练好的模型文件（.zip/.pth）
2. 提交到官方平台
3. 官方平台运行模型评测

---

## 规则要点

- ✅ 可用 PyTorch、TensorFlow 训练
- ✅ 可以用自己的 robotac 环境
- ❌ 只能用摄像头图像作为输入（不能预设坐标）
- ❌ 不能用规则硬编码替代AI决策
- ❌ 单次响应延迟 ≤100ms

---

## 常见问题

**Q: 官方只给嵌入式环境怎么训练？**

A: 规则允许自己配置开发环境，只需提交模型文件。

**Q: robotac环境连不上GitHub下assets？**

A: 从嵌入式环境复制 `metadrive/assets` 文件夹到 robotac 环境的 site-packages/metadrive/ 下。