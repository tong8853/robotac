# 后续改造计划

## 重要发现：比赛规则要求

**规则 4.1**：
> 仅接收摄像头实时图像数据作为输入，输出连续控制指令，禁止接入额外数据接口或外部网络资源。

**规则 4.2**：
> 不得预设仿真环境固定数据或任务节点坐标，需完全依赖实时视觉感知决策。

---

## 阶段一：摄像头图像观测适配 ✅ 完成

### 1.1 解决方案
实现了 `ImageObservationWrapper` 类，将 MetaDrive 图像格式转换为 SB3 兼容格式：
- 输入格式: `(H, W, C, history)` - 如 `(90, 160, 3, 3)`
- 输出格式: `(C*history, H, W)` - 如 `(9, 90, 160)`

### 1.2 训练结果
- **训练耗时**: 4847秒（约80分钟）
- **训练步数**: 200,000
- **策略网络**: MultiInputPolicy（适配 Dict 观测空间）
- **观测配置**: RGB摄像头 160x90，仅使用摄像头输入

### 1.3 训练期间表现
- 多次成功到达终点（arrive_dest）
- 最佳单次奖励: 502.11
- 训练后半段平均奖励: 200-350

### 1.4 测试结果
- **成功率**: 0/5（未能泛化到新场景）
- **主要问题**: 泛化能力不足
- **延迟合规**: 100%符合 ≤100ms 要求

---

## 阶段二：问题分析

### 2.1 泛化失败原因
1. **过拟合**: 模型记住了训练场景但无法泛化
2. **训练时长**: 200k 步对图像任务可能不够
3. **场景多样性**: 固定地图(block_num=3)限制了场景变化
4. **网络容量**: MultiInputPolicy 的 CNN 架构可能不够深

### 2.2 关键发现
- 训练时成功到达终点 → 模型确实学到了导航能力
- 测试时全部失败 → 泛化能力不足
- 违规统计显示压线频繁 → 策略不够平滑

---

## 阶段三：改进建议

### 3.1 短期改进（立即可行）
1. **增加训练步数**至 500k-1M
2. **地图随机化**: 使用 `generate_type: "big_block_sequence"` 生成多种地图
3. **课程学习**: 从简单场景开始，逐步增加难度
4. **调整超参数**: 减小学习率，增加 batch_size

### 3.2 中期改进
1. **增加环境多样性**: 多环境并行训练 (`n_envs=4`)
2. **数据增强**: 图像随机裁剪、颜色抖动
3. **更深的 CNN**: 使用 ResNet 风格的特征提取器
4. **正则化**: 增加 entropy_coef 鼓励探索

### 3.3 长期改进（比赛优化）
1. **导入预训练视觉模型**: 使用 ImageNet 预训练权重
2. **多任务学习**: 同时预测深度、语义分割辅助任务
3. **蒸馏学习**: 从教师模型（状态向量）到学生模型（图像）

---

## 阶段四：合规性确认

### 4.1 当前配置合规性
| 要求 | 状态 | 说明 |
|------|------|------|
| 仅使用 RGB 摄像头 | ✅ 符合 | image_observation=True |
| 无 Lidar | ✅ 符合 | show_lidar=False |
| 无导航标记 | ✅ 符合 | show_navi_mark=False |
| 推理延迟 ≤100ms | ✅ 符合 | 100% 合规 |
| 无外部网络 | ✅ 符合 | 完全离线运行 |

### 4.2 待验证
- [ ] 到达终点检测是否正常工作
- [ ] 违规惩罚是否正确触发
- [ ] 嵌入式环境兼容性

---

## 阶段五：下一步计划

### 5.1 立即行动
1. 使用地图随机化配置重新训练
2. 增加训练步数至 500k
3. 添加 TensorBoard 监控训练曲线

### 5.2 待办事项
- [ ] 编写地图随机化训练配置
- [ ] 实现课程学习策略
- [ ] 添加 wandb/tensorboard 监控
- [ ] 测试嵌入式环境部署

---

## 关键文件清单

| 文件 | 用途 | 状态 |
|------|------|------|
| `train_ppo.py` | 训练脚本 | 已更新支持图像观测 |
| `test_ppo.py` | 测试脚本 | 已同步 |
| `visualize_ppo.py` | 可视化脚本 | 已同步 |
| `reward_function.py` | 奖励函数 | 已就绪 |
| `observation_wrapper.py` | 图像格式转换 | 已实现 |
| `config.yaml` | 配置文件 | 待更新地图随机化 |

---

## 优先级排序

1. **最高**: 解决泛化问题（地图随机化 + 更多训练步数）
2. **高**: 添加训练监控（TensorBoard/wandb）
3. **中**: 课程学习实现
4. **低**: 嵌入式部署测试

---

## 训练日志摘要（图像观测版本）

```
迭代 73: 首次成功 arrive_dest
迭代 75: arrive_dest, 奖励 308.05, 889步, 0违规
迭代 112: arrive_dest, 奖励 435.35, 1268步, 0违规
迭代 180: arrive_dest, 奖励 441.62, 857步, 0违规
迭代 316: arrive_dest, 奖励 464.44, 515步, 0违规
迭代 362: arrive_dest, 奖励 388.37, 381步, 0违规
迭代 391: arrive_dest, 奖励 497.62, 490步, 0违规

训练完成: 200k步, 4847秒
最终测试: 0/5 成功（泛化失败）
```
